# vitepress-plugin-mermaid 会把 mermaid 的传递依赖（dayjs、cytoscape、d3 等）
# 注入 vite optimizeDeps.include，要求它们在顶层 node_modules 可解析。
# pnpm 默认的隔离结构不满足该假设，导致依赖预构建失败、mermaid 加载时
# dayjs default 导出缺失而白屏。提升到顶层以还原扁平结构兼容性。
shamefully-hoist=true
